apiVersion: v1
kind: ConfigMap
metadata:
  name: async-booking-system-config
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: booking_db
  DB_URL: jdbc:postgresql://postgres:5432/booking_db
  RABBITMQ_HOST: rabbitmq
  RABBITMQ_PORT: "5672"
  APP_PORT: "8080"
